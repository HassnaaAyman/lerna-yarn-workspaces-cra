{
  "name": "@lucifer/root",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope @lucifer/app -- yarn start"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
