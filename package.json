{
  "name": "@lucifer/root",
  "private": true,
  "devDependencies": {
    "@types/styled-components": "^5.1.25",
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope @lucifer/app -- yarn start",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "lerna exec --scope @lucifer/app -- yarn  build"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "publishConfig": {
    "registry": "http://localhost:3003"
  },
  "dependencies": {
    "antd": "^4.19.5",
    "axios": "^0.26.1",
    "react-google-login": "^5.2.2",
    "react-router-dom": "^6.3.0",
    "styled-components": "^5.3.5"
  }
}
